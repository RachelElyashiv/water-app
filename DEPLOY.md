# פריסה לענן — מדריך שלב-אחר-שלב

המטרה: שהאפליקציה תרוץ 24/7 בענן, עם כתובת אינטרנט קבועה (https),
כך שאפשר להיכנס מכל מכשיר, והתזכורות יעבדו גם כשהמחשב כבוי.

הפלטפורמה המומלצת: **Railway** — שרת תמיד-דלוק + דיסק קבוע לשמירת הנתונים.
(בהמשך יש גם הערה על Render החינמית.)

---

## מה כבר מוכן בקוד
- `Dockerfile` — הוראות בנייה לענן (הפלטפורמה בונה לבד).
- `.env` — קובץ **סודי** עם המפתחות שלך (כבר חסום מהעלאה ל-GitHub).
- הקוד תומך בדיסק קבוע דרך משתנה הסביבה `DATA_DIR`.
- קוקי מאובטח (secure) נדלק אוטומטית כש-`NODE_ENV=production`.

---

## שלב 1 — להעלות את הקוד ל-GitHub
צריך חשבון GitHub (חינמי). בתוך התיקייה הזו, בשורת פקודה:

```
git init
git add .
git commit -m "Water hydration app"
```

צרי מאגר (repository) חדש וריק ב-GitHub, ואז:

```
git remote add origin https://github.com/<שם-המשתמש-שלך>/water-app.git
git branch -M main
git push -u origin main
```

> הקובץ הסודי `.env` ותיקיית `data/` **לא** יועלו — זה בכוונה.

---

## שלב 2 — ליצור שירות ב-Railway
1. היכנסי ל-https://railway.app והתחברי עם GitHub.
2. **New Project → Deploy from GitHub repo** ובחרי את המאגר שיצרת.
3. Railway יזהה את ה-`Dockerfile` ויבנה אוטומטית.

## שלב 3 — משתני סביבה (Variables)
בשירות שנוצר → לשונית **Variables** → הוסיפי את הערכים מתוך הקובץ `.env`:

| שם | ערך |
|----|-----|
| `NODE_ENV` | `production` |
| `JWT_SECRET` | (העתיקי מתוך `.env`) |
| `VAPID_PUBLIC_KEY` | (העתיקי מתוך `.env`) |
| `VAPID_PRIVATE_KEY` | (העתיקי מתוך `.env`) |
| `DATA_DIR` | `/data` |

## שלב 4 — דיסק קבוע (Volume) — חשוב לשמירת הנתונים
בשירות → **Settings → Volumes → New Volume**, ומיפוי הנתיב (Mount Path) ל:
```
/data
```
בלי זה — הנתונים יימחקו בכל פריסה מחדש.

## שלב 5 — כתובת ציבורית
בשירות → **Settings → Networking → Generate Domain**.
תקבלי כתובת כמו `https://water-app-production.up.railway.app` — זו האפליקציה שלך.

---

## אחרי הפריסה
- פותחים את הכתובת בטלפון → הרשמה → "הוסף למסך הבית" (מתקינים כאפליקציה).
- בהגדרות → מפעילים תזכורות ומאשרים התראות. הן יגיעו גם כשהטלפון נעול.
- לעדכון עתידי: `git add . && git commit -m "..." && git push` — Railway יפרוס לבד.

## עלות
Railway נותנת קרדיט התחלתי; שירות קטן כזה עולה בערך ‎$5 לחודש בהמשך.

---

## חלופה חינמית — Render (עם הסתייגות)
ב-https://render.com אפשר **New → Web Service** מ-GitHub, עם אותם משתני סביבה.
חשוב לדעת:
- התוכנית החינמית **נרדמת** אחרי ~15 דקות חוסר פעילות → **תזכורות לא אמינות**.
- דיסק קבוע ב-Render הוא בתשלום; בלעדיו הנתונים נמחקים בכל פריסה.
לכן למי שרוצה שהתזכורות יעבדו באמת — Railway עדיפה.
